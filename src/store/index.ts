import Vue from 'vue';
import Vuex from 'vuex';
import OverlayFeedback from './OverlayFeedback';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    OverlayFeedback,
  },
});
